const { BigNumber } = require('alchemy-sdk');

const ZERO_BN = BigNumber.from(0);
const TEN_BN = BigNumber.from(10);

const MILESTONE = {
  siloV3: 17671557
};

module.exports = {
  ZERO_BN,
  TEN_BN,
  MILESTONE
};
